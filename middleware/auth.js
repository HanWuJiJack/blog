export default function ({
  store,
  route,
  redirect
}) {
  // console.log(route)
  // if (!store.state.user.whileList.includes(route.path) && !store.state.token) {
  //   return redirect('/login')
  // }
  // if (store.state.user.whileList.includes(route.path) && store.state.token) {
  //   return redirect('/')
  // }
}
